# Local development — copy this file to .env.local and set your values
# DATABASE_URL is set via Vercel / GitHub Actions in production

# For local development, set your Neon connection string:
# DATABASE_URL="postgresql://..."
