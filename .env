# BetMines Double Tracker — Environment Variables
# No secrets needed for local use.

# Prisma SQLite path (relative to /prisma folder)
DATABASE_URL="file:./betmines.db"

# Optional: set to "development" for verbose Prisma logs
NODE_ENV=development
